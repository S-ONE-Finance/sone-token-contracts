{
  "name": "@s-one-finance/core",
  "description": "🛠 Smart Contract for S-ONE Finance Swap.",
  "version": "1.0.0",
  "main": "index.js",
  "files": [
    "build",
    "contracts"
  ],
  "license": "MIT",
  "repository": "https://github.com/S-ONE-Finance/soneswap-core.git",
  "keywords": [
    "soneswap",
    "ethereum",
    "uniswap",
    "sushiswap"
  ],
  "dependencies": {
    "@openzeppelin/contracts": "^4.0.0",
    "@openzeppelin/contracts-upgradeable": "^4.0.0",
    "@openzeppelin/test-helpers": "^0.5.10",
    "@truffle/hdwallet-provider": "^1.2.1",
    "dotenv": "^8.2.0",
    "mocha": "^8.3.2",
    "rimraf": "^3.0.2",
    "truffle": "^5.1.44",
    "truffle-flattener": "^1.4.4",
    "truffle-hdwallet-provider": "^1.0.17"
  },
  "scripts": {
    "precompile": "rimraf ./build/",
    "compile": "yarn truffle compile",
    "flatten": "mkdir -p build/flatten-contracts && yarn truffle-flattener",
    "deploy:ethereum": "yarn truffle migrate --network ethereum",
    "deploy:local": "yarn truffle migrate --network dev",
    "deploy:ropsten": "yarn truffle migrate --network ropsten",
    "deploy:rinkeby": "yarn truffle migrate --network rinkeby",
    "deploy:bsc": "yarn truffle migrate --network bsc",
    "deploy:bsc_testnet": "yarn truffle migrate --network bsc_testnet",
    "test": "yarn truffle test"
  },
  "devDependencies": {
    "@openzeppelin/truffle-upgrades": "^1.5.1"
  }
}
